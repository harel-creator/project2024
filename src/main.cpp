// The code that handles launching and running the Bloom Filter application.

#include "BloomFilterApp.h"

int main(int argc, char** argv) {
    BloomFilterApp app;
    app.setUp();
    app.run();

    return 0;
}
